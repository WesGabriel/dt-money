import { Header } from "../../components/Header";
import { Summary } from "../Summary";

export const Transactions = () => {
  return (
    <div>
      <Header />
      <Summary />
    </div>
  );
};
